<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoteApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/toolbar_navigation_color</item>
        <item name="android:navigationBarColor">@color/toolbar_navigation_color</item>

    </style>

    <style name="Theme.NoteApp" parent="Base.Theme.NoteApp" />
</resources>